import ValidatedCreateForm from "./ValidatedCreateForm";

const Create = () => {
  return (
    <section id="create-page">
      <ValidatedCreateForm />
    </section>
  );
};

export default Create;
